apiVersion: v1
kind: Service
metadata:
  name: kvstore-master
  namespace: kvstore
spec:
  type: LoadBalancer  # or ClusterIP for internal only
  selector:
    app: kvstore
    role: master
  ports:
  - name: kvstore
    port: 5555
    targetPort: 5555
